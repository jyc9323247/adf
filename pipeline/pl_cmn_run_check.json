{
	"name": "pl_cmn_run_check",
	"properties": {
		"description": "공통 자식 (v8.3): 마스터 2번째 단계. 전역 skip / BIZDAY 판정 -> ctl_trigger_history 기록, SKIP 이면 Master = SKIPPED",
		"activities": [
			{
				"name": "If_bizday",
				"description": "BIZDAY 판정 대상(p_bizday_n 있음, Manual 아님, 전역 skip 아님)일 때만 고객 DB2 조회",
				"type": "IfCondition",
				"dependsOn": [],
				"userProperties": [],
				"typeProperties": {
					"expression": {
						"value": "@and(and(not(empty(pipeline().parameters.p_bizday_n)), not(equals(toLower(pipeline().parameters.p_trigger_type), 'manual'))), not(pipeline().globalParameters.g_skip_yn))",
						"type": "Expression"
					},
					"ifTrueActivities": [
						{
							"name": "Lkp_biz_cnt",
							"description": "당월 영업일 수 (0 = CALENDAR_MISSING)",
							"type": "Lookup",
							"dependsOn": [],
							"policy": {
								"timeout": "0.00:10:00",
								"retry": 1,
								"retryIntervalInSeconds": 30,
								"secureOutput": false,
								"secureInput": false
							},
							"userProperties": [],
							"typeProperties": {
								"source": {
									"type": "Db2Source",
									"query": {
										"value": "SELECT COUNT(*) AS BIZ_CNT\n  FROM MLDADM.TBCMB1001\n WHERE HLDY_CD = '00'\n   AND BZ_YM = VARCHAR_FORMAT(CURRENT DATE, 'YYYYMM')",
										"type": "Expression"
									}
								},
								"firstRowOnly": true
							}
						},
						{
							"name": "Lkp_bizday",
							"description": "영업일 판정 (RUN_YN / BASE_BIZ_DT / EXEC_DT)",
							"type": "Lookup",
							"dependsOn": [
								{
									"activity": "Lkp_biz_cnt",
									"dependencyConditions": [
										"Succeeded"
									]
								}
							],
							"policy": {
								"timeout": "0.00:10:00",
								"retry": 1,
								"retryIntervalInSeconds": 30,
								"secureOutput": false,
								"secureInput": false
							},
							"userProperties": [],
							"typeProperties": {
								"source": {
									"type": "Db2Source",
									"query": {
										"value": "WITH PARAM AS\n(\n    SELECT\n        @{int(pipeline().parameters.p_bizday_n)} AS BIZDAY_N,\n        @{if(empty(pipeline().parameters.p_bizday_offset), 0, int(pipeline().parameters.p_bizday_offset))} AS BIZDAY_OFFSET\n    FROM SYSIBM.SYSDUMMY1\n),\nBIZ_DAY AS\n(\n    SELECT\n        Y.BZ_YM,\n        Y.SOLR_DT,\n\n        ROW_NUMBER() OVER\n        (\n            PARTITION BY Y.BZ_YM\n            ORDER BY Y.SOLR_DT\n        ) AS BIZ_DAY_SEQ\n\n    FROM MLDADM.TBCMB1001 Y\n\n    WHERE Y.HLDY_CD = '00'\n\n      /* offset으로 월 경계를 넘어가는 경우까지 고려 */\n      AND Y.BZ_YM IN\n      (\n          VARCHAR_FORMAT(CURRENT DATE - 1 MONTH, 'YYYYMM'),\n          VARCHAR_FORMAT(CURRENT DATE,           'YYYYMM'),\n          VARCHAR_FORMAT(CURRENT DATE + 1 MONTH, 'YYYYMM')\n      )\n),\nTARGET_DAY AS\n(\n    SELECT\n        B.BZ_YM,\n\n        B.SOLR_DT AS BASE_BIZ_DT,\n\n        DATE(\n            TIMESTAMP_FORMAT(B.SOLR_DT, 'YYYYMMDD')\n        )\n        + P.BIZDAY_OFFSET DAYS AS EXEC_DATE\n\n    FROM BIZ_DAY B\n\n    CROSS JOIN PARAM P\n\n    WHERE B.BIZ_DAY_SEQ = P.BIZDAY_N\n)\nSELECT\n    CASE\n        WHEN COUNT(\n            CASE\n                WHEN EXEC_DATE = CURRENT DATE\n                THEN 1\n            END\n        ) > 0\n        THEN 'Y'\n        ELSE 'N'\n    END AS RUN_YN,\n\n    MAX(\n        CASE\n            WHEN EXEC_DATE = CURRENT DATE\n            THEN BASE_BIZ_DT\n        END\n    ) AS BASE_BIZ_DT,\n\n    MAX(\n        CASE\n            WHEN EXEC_DATE = CURRENT DATE\n            THEN VARCHAR_FORMAT(EXEC_DATE, 'YYYYMMDD')\n        END\n    ) AS EXEC_DT\n\nFROM TARGET_DAY",
										"type": "Expression"
									}
								},
								"firstRowOnly": true
							}
						},
						{
							"name": "Set_biz_result",
							"description": "조회 결과를 | 구분 문자열로 저장 (If 밖 Script 에서 사용)",
							"type": "SetVariable",
							"dependsOn": [
								{
									"activity": "Lkp_bizday",
									"dependencyConditions": [
										"Succeeded"
									]
								}
							],
							"policy": {
								"secureOutput": false,
								"secureInput": false
							},
							"userProperties": [],
							"typeProperties": {
								"variableName": "v_biz_result",
								"value": {
									"value": "@concat(string(activity('Lkp_biz_cnt').output.firstRow.BIZ_CNT), '|', coalesce(activity('Lkp_bizday').output.firstRow.RUN_YN, ''), '|', coalesce(activity('Lkp_bizday').output.firstRow.BASE_BIZ_DT, ''), '|', coalesce(activity('Lkp_bizday').output.firstRow.EXEC_DT, ''))",
									"type": "Expression"
								}
							}
						},
						{
							"name": "Set_biz_err",
							"description": "조회 실패 표시 -> Scr_judge 가 ETC 로 기록",
							"type": "SetVariable",
							"dependsOn": [
								{
									"activity": "Lkp_bizday",
									"dependencyConditions": [
										"Failed",
										"Skipped"
									]
								}
							],
							"policy": {
								"secureOutput": false,
								"secureInput": false
							},
							"userProperties": [],
							"typeProperties": {
								"variableName": "v_biz_err",
								"value": "Y"
							}
						}
					]
				}
			},
			{
				"name": "Scr_judge",
				"description": "판정 결과 -> Trigger 이력 + Master SKIPPED",
				"type": "Script",
				"dependsOn": [
					{
						"activity": "If_bizday",
						"dependencyConditions": [
							"Succeeded"
						]
					}
				],
				"policy": {
					"timeout": "0.00:10:00",
					"retry": 0,
					"retryIntervalInSeconds": 30,
					"secureOutput": false,
					"secureInput": false
				},
				"userProperties": [],
				"typeProperties": {
					"scripts": [
						{
							"type": "Query",
							"text": {
								"value": "/* pl_cmn_run_check > Script \"Scr_judge\" (v8.3)\n   전역 skip / BIZDAY 판정 결과 -> ctl_trigger_history 1행 (master_run_id 당 1회) + Master PENDING -> SKIPPED\n   판정 순서 : GLOBAL_SKIP -> ETC (고객 DB 조회 실패) -> CALENDAR_MISSING -> NOT_BIZDAY -> 실행 (run_yn = Y, Master 는 PENDING 유지)\n   BIZDAY 판정 대상 : p_bizday_n 이 있고 trigger_type 이 Manual 이 아닌 실행\n   ETC : Master 는 SKIPPED 로 바꾸지 않음 (skip_reason_cd 제약). 이후 Fail 활동으로 파이프라인 실패 -> 마스터가 Master FAILED 처리\n   같은 master_run_id 재호출 시 이력 중복 INSERT 안 함. ADF 동적 식은 p CTE 에만 사용. 주석은 블록 주석만 사용 */\nWITH p AS (\n    SELECT NULLIF(trim('@{pipeline().parameters.p_master_run_id}'), '')                    AS master_run_id,\n           COALESCE(NULLIF(trim('@{pipeline().parameters.p_trigger_nm}'), ''), 'Manual')    AS trigger_nm,\n           NULLIF(trim('@{pipeline().parameters.p_trigger_id}'), '')                       AS trigger_run_id,\n           NULLIF(trim('@{pipeline().parameters.p_scheduled_time}'), '')::timestamptz      AS scheduled_dt,\n           NULLIF(trim('@{pipeline().parameters.p_requester}'), '')                        AS requester_nm,\n           '@{if(pipeline().globalParameters.g_skip_yn, 'Y', 'N')}'::text                  AS global_skip_yn,\n           (NULLIF(trim('@{pipeline().parameters.p_bizday_n}'), '') IS NOT NULL\n            AND lower(trim('@{pipeline().parameters.p_trigger_type}')) <> 'manual')        AS bizday_yn,\n           '@{variables('v_biz_err')}'::text                                               AS biz_err,\n           '@{variables('v_biz_result')}'::text                                            AS biz_result,\n           '@{pipeline().DataFactory}'::text                                               AS created_by\n),\n/* v_biz_result = 당월 영업일 수 | RUN_YN | BASE_BIZ_DT | EXEC_DT */\nb AS (\n    SELECT p.*,\n           NULLIF(btrim(split_part(p.biz_result, '|', 1)), '')::int AS biz_cnt,\n           NULLIF(btrim(split_part(p.biz_result, '|', 2)), '')      AS biz_run_yn,\n           NULLIF(btrim(split_part(p.biz_result, '|', 3)), '')      AS base_biz_dt,\n           NULLIF(btrim(split_part(p.biz_result, '|', 4)), '')      AS exec_dt\n      FROM p\n),\nd AS (\n    SELECT b.*, m.run_group_id,\n           CASE WHEN b.global_skip_yn = 'Y'                                       THEN 'GLOBAL_SKIP'\n                WHEN b.bizday_yn AND b.biz_err = 'Y'                              THEN 'ETC'\n                WHEN b.bizday_yn AND COALESCE(b.biz_cnt, 0) = 0                   THEN 'CALENDAR_MISSING'\n                WHEN b.bizday_yn AND COALESCE(b.biz_run_yn, 'N') <> 'Y'           THEN 'NOT_BIZDAY'\n           END AS skip_reason_cd\n      FROM b\n      JOIN adfadm.ctl_master_pipeline_run m ON m.master_run_id = b.master_run_id\n),\nh AS (\n    INSERT INTO adfadm.ctl_trigger_history\n          (adf_trigger_nm, adf_trigger_run_id, scheduled_dt, run_group_id, master_run_id,\n           run_yn, skip_reason_cd, skip_msg, base_biz_dt, exec_dt, requester_nm, created_by)\n    SELECT d.trigger_nm, d.trigger_run_id, d.scheduled_dt, d.run_group_id, d.master_run_id,\n           CASE WHEN d.skip_reason_cd IS NULL THEN 'Y' ELSE 'N' END,\n           d.skip_reason_cd,\n           CASE d.skip_reason_cd\n                WHEN 'GLOBAL_SKIP'      THEN '전역 skip (g_skip_yn = true)'\n                WHEN 'CALENDAR_MISSING' THEN '고객 영업일 데이터 없음 (MLDADM.TBCMB1001 당월 0건)'\n                WHEN 'NOT_BIZDAY'       THEN '오늘은 지정한 영업일 (+offset) 이 아님'\n                WHEN 'ETC'              THEN '고객 DB 영업일 조회 실패 (Lkp_biz_cnt / Lkp_bizday). ADF 모니터에서 RunId 확인'\n           END,\n           d.base_biz_dt, d.exec_dt, d.requester_nm, d.created_by\n      FROM d\n     WHERE NOT EXISTS (SELECT 1 FROM adfadm.ctl_trigger_history x WHERE x.master_run_id = d.master_run_id)\n    RETURNING trigger_hist_id\n),\nu AS (\n    UPDATE adfadm.ctl_master_pipeline_run m\n       SET status         = CASE WHEN d.skip_reason_cd IN ('GLOBAL_SKIP','CALENDAR_MISSING','NOT_BIZDAY')\n                                 THEN 'SKIPPED' ELSE m.status END,\n           skip_reason_cd = CASE WHEN d.skip_reason_cd IN ('GLOBAL_SKIP','CALENDAR_MISSING','NOT_BIZDAY')\n                                 THEN d.skip_reason_cd END,\n           end_dt         = CASE WHEN d.skip_reason_cd IN ('GLOBAL_SKIP','CALENDAR_MISSING','NOT_BIZDAY')\n                                 THEN now() ELSE m.end_dt END,\n           base_biz_dt    = COALESCE(d.base_biz_dt, m.base_biz_dt),\n           updated_by     = d.created_by,\n           updated_dt     = now()\n      FROM d\n     WHERE m.master_run_id = d.master_run_id\n       AND m.status = 'PENDING'\n    RETURNING m.master_run_id, m.status\n)\nSELECT d.skip_reason_cd,\n       CASE WHEN d.skip_reason_cd IS NULL THEN 'Y' ELSE 'N' END AS run_yn,\n       d.base_biz_dt, d.exec_dt,\n       (SELECT count(*) FROM h) AS hist_ins_cnt,\n       (SELECT count(*) FROM u WHERE status = 'SKIPPED') AS master_skipped_cnt,\n       CASE WHEN (SELECT count(*) FROM d) = 0\n            THEN ('MASTER_NOT_FOUND:' || (SELECT COALESCE(master_run_id, '') FROM p))::int\n            ELSE 1 END AS chk\n  FROM (SELECT 1) x\n  LEFT JOIN d ON TRUE;\n",
								"type": "Expression"
							}
						}
					],
					"scriptBlockExecutionTimeout": "00:10:00"
				}
			},
			{
				"name": "If_etc",
				"description": "ETC (DB2 조회 실패) 이면 파이프라인 실패 -> 마스터가 Master FAILED 처리",
				"type": "IfCondition",
				"dependsOn": [
					{
						"activity": "Scr_judge",
						"dependencyConditions": [
							"Succeeded"
						]
					}
				],
				"userProperties": [],
				"typeProperties": {
					"expression": {
						"value": "@equals(coalesce(activity('Scr_judge').output.resultSets[0].rows[0].skip_reason_cd, ''), 'ETC')",
						"type": "Expression"
					},
					"ifTrueActivities": [
						{
							"name": "Fail_etc",
							"type": "Fail",
							"dependsOn": [],
							"userProperties": [],
							"typeProperties": {
								"message": "고객 DB 영업일 조회 실패 (Lkp_biz_cnt / Lkp_bizday). Trigger 이력 ETC 기록됨",
								"errorCode": "RUN_CHECK_ETC"
							}
						}
					]
				}
			}
		],
		"parameters": {
			"p_master_run_id": {
				"type": "String"
			},
			"p_master_pipeline_nm": {
				"type": "String"
			},
			"p_trigger_type": {
				"type": "String"
			},
			"p_trigger_nm": {
				"type": "String"
			},
			"p_trigger_id": {
				"type": "String"
			},
			"p_run_group_nm": {
				"type": "String"
			},
			"p_scheduled_time": {
				"type": "String"
			},
			"p_bizday_n": {
				"type": "String"
			},
			"p_bizday_offset": {
				"type": "String"
			},
			"p_requester": {
				"type": "String"
			},
			"p_calendar_db_nm": {
				"type": "String",
				"defaultValue": "MLDADM"
			}
		},
		"variables": {
			"v_biz_result": {
				"type": "String"
			},
			"v_biz_err": {
				"type": "String",
				"defaultValue": "N"
			}
		},
		"folder": {
			"name": "util/sop"
		},
		"annotations": []
	}
}